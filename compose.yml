services:
  raus:
    build: .
    container_name: raus
    restart: unless-stopped
    expose:
      - "5000"
    volumes:
      - ./data:/app/data
    networks:
      - proxy

networks:
  proxy:
    external: true
